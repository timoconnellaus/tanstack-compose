# Shared agent example runtime

Example-local conversation loop, session, registries and model wrappers used by
the browser-only and deployed agent demonstrations. This is intentionally a
private workspace package: Compose's public agent-facing surface is
`@tanstack/compose-tools`.
